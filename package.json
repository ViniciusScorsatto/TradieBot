{
  "name": "invoicebot",
  "private": true,
  "packageManager": "npm@10.8.1",
  "engines": {
    "node": ">=20.19.0"
  },
  "workspaces": [
    "dashboard",
    "site",
    "packages/shared"
  ],
  "scripts": {
    "dev:dashboard": "npm run dev --workspace dashboard",
    "dev:site": "npm run dev --workspace site",
    "build:dashboard": "npm run prisma:generate && npm run build --workspace @invoicebot/shared && npm run build --workspace dashboard",
    "build:site": "npm run build --workspace @invoicebot/shared && npm run build --workspace site",
    "start:dashboard": "npm run start --workspace dashboard",
    "start:site": "npm run start --workspace site",
    "build": "npm run prisma:generate && npm run build --workspace @invoicebot/shared && npm run build --workspace dashboard && npm run build --workspace site",
    "lint": "npm run lint --workspace dashboard && npm run lint --workspace site",
    "test:dashboard": "node --test dashboard/tests/*.test.js",
    "test:bot": "PYTHONPATH=bot python3 -m unittest tests.test_parser tests.test_invoice_pdf tests.test_compliance tests.test_support_ai tests.test_retention_policy tests.test_promotions_consent",
    "test": "npm run test:dashboard && npm run test:bot",
    "prisma:generate": "npx prisma generate",
    "prisma:migrate": "npx prisma migrate dev",
    "prisma:deploy": "npx prisma migrate deploy"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "dotenv": "^16.6.1",
    "prisma": "^7.6.0",
    "typescript": "^5.6.3"
  },
  "overrides": {
    "styled-jsx": {
      "react": "^18.3.1"
    }
  }
}
